# KSpace-AI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar el stub fake del endpoint `/evaluate` por un LLM real (Gemini, brandeado como **KSpace-AI**) y agregar al Calendario una lectura mensual del estado del árbol + felicitaciones template cuando el usuario agrega actividades a sefirot flojas.

**Architecture:** Un módulo `backend/llm.py` (clase `KSpaceAi`) encapsula todas las llamadas a Gemini con fallback automático al stub si la API falla o el usuario tiene el toggle apagado. Dos endpoints nuevos (`/ia/calendario/lectura` y `/ia/calendario/felicitacion`) consumen ese servicio. El frontend agrega una card en el Calendario y un toast post-creación de actividad. Toggle de opt-out por usuario (columna `ksai_enabled` en `usuarios`).

**Tech Stack:** FastAPI + SQLAlchemy async + Alembic + SQLite/Postgres. Gemini 2.5 Flash vía `google-genai` SDK. Frontend React 19 + framer-motion 11 + Tailwind 4.

**Spec:** `docs/superpowers/specs/2026-05-21-kspace-ai-design.md`

---

## File Map

**Backend (new):**
- `backend/llm.py` — Clase `KSpaceAi`. Wrapper sobre Gemini con fallback al stub.
- `backend/tests/test_llm.py` — Tests unitarios de `KSpaceAi` (mock del cliente Gemini).
- `backend/tests/test_ksai_endpoints.py` — Tests integración endpoints /ia/calendario/* y PATCH /usuarios/me/ksai.
- `backend/alembic/versions/<hash>_add_ksai_enabled.py` — Migration que agrega columna.

**Backend (modify):**
- `backend/requirements.txt` — Sumar `google-genai>=0.3,<1.0`.
- `backend/config.py` — Agregar `gemini_api_key: str = ""`.
- `backend/models.py` — Agregar `ksai_enabled` a Usuario.
- `backend/auth.py` — Agregar `ksai_enabled: bool = True` a `UserOut`.
- `backend/main.py` — Endpoint `/evaluate` usa `KSpaceAi`. Tres endpoints nuevos.
- `backend/.env.example` — Agregar `GEMINI_API_KEY=`.

**Frontend (new):**
- `frontend/src/calendar/components/CalendarioIaLectura.tsx` — Card al tope del Calendario.
- `frontend/src/calendar/hooks/useIaLectura.ts` — Hook que fetcha `/ia/calendario/lectura`.
- `frontend/src/calendar/hooks/useFelicitacion.ts` — Helper para disparar `/ia/calendario/felicitacion` post-creación.
- `frontend/src/calendar/components/FelicitacionToast.tsx` — Toast top-right.

**Frontend (modify):**
- `frontend/src/calendar/CalendarModule.tsx` — Renderiza `<CalendarioIaLectura />` y el toast.
- `frontend/src/calendar/components/ActivityForm.tsx` — Tras 201, dispara felicitación.
- `frontend/src/auth/UserMenu.tsx` — Toggle "KSpace-AI: on/off" + sección "Configuración".
- `frontend/src/auth/api.ts` — Helper `updateKsai(enabled)`.
- `frontend/src/auth/types.ts` — `User` gana `ksai_enabled: boolean`.
- `frontend/src/espejo/components/ReflectionEditor.tsx` — Nota debajo del textarea.

---

### Task 1: Sumar dependencia `google-genai` al backend

**Files:**
- Modify: `backend/requirements.txt`

- [ ] **Step 1: Agregar dependencia**

Append a `backend/requirements.txt`:

```
google-genai>=0.3,<1.0
```

- [ ] **Step 2: Instalar en el venv**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pip install "google-genai>=0.3,<1.0"
```

Expected: instalación exitosa, sin downgrade de otros paquetes.

- [ ] **Step 3: Verificar import**

Run:

```bash
./venv/Scripts/python.exe -c "from google import genai; print(genai.__version__)"
```

Expected: imprime una versión >= 0.3 sin error.

- [ ] **Step 4: Commit**

```bash
git add backend/requirements.txt
git commit -m "build(backend): agregar dep google-genai para KSpace-AI"
```

---

### Task 2: Agregar `gemini_api_key` a config y `.env.example`

**Files:**
- Modify: `backend/config.py`
- Modify: `backend/.env.example`

- [ ] **Step 1: Agregar setting**

En `backend/config.py`, dentro de la clase `Settings`, ubicar el bloque LLM y reemplazar:

```python
    # ---------- LLM (used by issue #9) ----------
    # "stub" | "claude" | "gemini"
    llm_provider: str = "stub"
    llm_api_key: str = ""
```

por:

```python
    # ---------- LLM / KSpace-AI ----------
    # "stub" | "gemini"
    llm_provider: str = "stub"
    gemini_api_key: str = ""

    @property
    def ksai_configured(self) -> bool:
        return self.llm_provider == "gemini" and bool(self.gemini_api_key)
```

(Reemplazamos `llm_api_key` por `gemini_api_key` y eliminamos `claude` del comentario porque no está en scope.)

- [ ] **Step 2: Actualizar .env.example**

En `backend/.env.example`, reemplazar la línea existente de `LLM_API_KEY` (si está) o agregar al final:

```
# KSpace-AI (Gemini)
LLM_PROVIDER=stub
GEMINI_API_KEY=
```

- [ ] **Step 3: Verificar config carga**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -c "from config import get_settings; s = get_settings(); print('ksai_configured:', s.ksai_configured, 'provider:', s.llm_provider)"
```

Expected: `ksai_configured: False provider: stub` (sin API key seteada).

- [ ] **Step 4: Commit**

```bash
git add backend/config.py backend/.env.example
git commit -m "feat(backend): config para KSpace-AI (gemini_api_key + ksai_configured)"
```

---

### Task 3: Migración Alembic — agregar columna `ksai_enabled` a `usuarios`

**Files:**
- Create: `backend/alembic/versions/<auto>_add_ksai_enabled.py`
- Modify: `backend/models.py`

- [ ] **Step 1: Agregar la columna al modelo**

En `backend/models.py`, dentro de la clase `Usuario`, después de la línea `gcal_sync_enabled = ...` agregar:

```python
    ksai_enabled             = Column(Boolean, nullable=False, default=True, server_default="true")
```

Nota: `server_default="true"` se materializa en SQLite como el string truthy `"true"`. Mantenemos el `default=True` a nivel Python para que las inserciones desde el ORM no dependan del server_default.

- [ ] **Step 2: Generar la migration**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m alembic revision --autogenerate -m "add ksai_enabled to usuarios"
```

Expected: crea un archivo `backend/alembic/versions/<hash>_add_ksai_enabled.py` con un `op.add_column('usuarios', sa.Column('ksai_enabled', sa.Boolean(), server_default='true', nullable=False))` en `upgrade()` y el `op.drop_column` en `downgrade()`.

- [ ] **Step 3: Aplicar la migration**

Run:

```bash
./venv/Scripts/python.exe -m alembic upgrade head
```

Expected: "Running upgrade ... -> <hash>, add ksai_enabled to usuarios".

- [ ] **Step 4: Verificar con un round-trip simple**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -c "
import asyncio
from sqlalchemy import select
from database import AsyncSessionLocal
from models import Usuario
async def main():
    async with AsyncSessionLocal() as s:
        rows = (await s.execute(select(Usuario).limit(1))).scalars().all()
        for u in rows:
            print(u.email, 'ksai_enabled=', u.ksai_enabled)
asyncio.run(main())
"
```

Expected: imprime al menos un usuario con `ksai_enabled= True` (o no imprime nada si no hay users — también vale).

- [ ] **Step 5: Commit**

```bash
git add backend/models.py backend/alembic/versions/*_add_ksai_enabled.py
git commit -m "feat(backend): columna ksai_enabled en usuarios (default true)"
```

---

### Task 4: Esqueleto de `KSpaceAi` con fallback stub

**Files:**
- Create: `backend/llm.py`
- Create: `backend/tests/test_llm.py`

- [ ] **Step 1: Test failing — instanciar servicio en modo stub**

Crear `backend/tests/test_llm.py` con:

```python
import pytest
from llm import KSpaceAi


def test_kspace_ai_stub_mode_evaluate_reflection():
    """Cuando provider != 'gemini' o falta api_key, evaluate_reflection cae al stub."""
    svc = KSpaceAi(provider="stub", api_key="")
    score, feedback = svc.evaluate_reflection_sync(
        sefira_nombre="Tiféret", texto="Hoy intenté equilibrar.", user_score=6.0,
    )
    assert 1.0 <= score <= 10.0
    assert isinstance(feedback, str) and len(feedback) > 0


def test_kspace_ai_stub_mode_calendar_reading():
    """En modo stub, generate_calendar_reading devuelve None (frontend oculta texto)."""
    svc = KSpaceAi(provider="stub", api_key="")
    result = svc.generate_calendar_reading_sync([("Tiféret", 4.2)])
    assert result is None
```

- [ ] **Step 2: Correr para ver que falla**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_llm.py -v
```

Expected: ImportError "No module named 'llm'".

- [ ] **Step 3: Implementar el esqueleto**

Crear `backend/llm.py`:

```python
"""KSpace-AI: wrapper sobre Gemini para evaluar reflexiones y leer el árbol.

Diseño:
- Una instancia configurada con (provider, api_key). Si provider != "gemini"
  o falta api_key, todas las llamadas devuelven fallbacks deterministas.
- Las llamadas reales a Gemini van a implementarse en una próxima tarea.

Cada función tiene dos variantes: la async (la que usa FastAPI) y un
`*_sync` que existe solo para tests sincrónicos en este módulo aislado.
"""
from __future__ import annotations

import random
from typing import Optional


STUB_FEEDBACK = (
    "KSpace-AI no está disponible en este momento. "
    "Tu reflexión fue guardada y se evaluará cuando vuelva el servicio."
)


class KSpaceAi:
    def __init__(self, provider: str, api_key: str):
        self.provider = provider
        self.api_key = api_key

    @property
    def enabled(self) -> bool:
        return self.provider == "gemini" and bool(self.api_key)

    # ---- Sync versions (para tests + composability) ----

    def evaluate_reflection_sync(
        self, sefira_nombre: str, texto: str, user_score: float,
    ) -> tuple[float, str]:
        if not self.enabled:
            return self._stub_evaluate(user_score)
        # Real call irá en la próxima tarea
        return self._stub_evaluate(user_score)

    def generate_calendar_reading_sync(
        self, sefirot_debiles: list[tuple[str, float]],
    ) -> Optional[str]:
        if not self.enabled:
            return None
        # Real call irá en la próxima tarea
        return None

    # ---- Async versions (las que usa FastAPI) ----

    async def evaluate_reflection(
        self, sefira_nombre: str, texto: str, user_score: float,
    ) -> tuple[float, str]:
        return self.evaluate_reflection_sync(sefira_nombre, texto, user_score)

    async def generate_calendar_reading(
        self, sefirot_debiles: list[tuple[str, float]],
    ) -> Optional[str]:
        return self.generate_calendar_reading_sync(sefirot_debiles)

    # ---- Stub helpers ----

    @staticmethod
    def _stub_evaluate(user_score: float) -> tuple[float, str]:
        jitter = random.choice([-1.5, -0.5, 0.0, 0.5, 1.5])
        score = max(1.0, min(10.0, user_score + jitter))
        return score, STUB_FEEDBACK
```

- [ ] **Step 4: Correr los tests para verificar que pasan**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_llm.py -v
```

Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add backend/llm.py backend/tests/test_llm.py
git commit -m "feat(backend): esqueleto de KSpaceAi con fallback stub"
```

---

### Task 5: Implementar `evaluate_reflection` real contra Gemini

**Files:**
- Modify: `backend/llm.py`
- Modify: `backend/tests/test_llm.py`

- [ ] **Step 1: Test failing — llamada real con cliente mockeado**

Agregar a `backend/tests/test_llm.py`:

```python
import json
from unittest.mock import MagicMock
import pytest


@pytest.mark.asyncio
async def test_evaluate_reflection_calls_gemini_and_parses_json():
    """En modo gemini, llama al cliente y parsea el JSON de respuesta."""
    # Mock que simula el cliente google-genai
    fake_client = MagicMock()
    fake_resp = MagicMock()
    fake_resp.text = json.dumps({
        "score": 7.5,
        "feedback": "Tu reflexión muestra una búsqueda de equilibrio.",
    })
    fake_client.aio.models.generate_content = MagicMock(
        return_value=_async_return(fake_resp)
    )

    svc = KSpaceAi(provider="gemini", api_key="fake-key", client=fake_client)
    score, feedback = await svc.evaluate_reflection(
        sefira_nombre="Tiféret", texto="Hoy intenté equilibrar.", user_score=6.0,
    )

    assert score == 7.5
    assert "equilibrio" in feedback.lower()


@pytest.mark.asyncio
async def test_evaluate_reflection_falls_back_on_exception():
    """Si el cliente tira excepción (timeout, rate limit, etc), cae al stub."""
    fake_client = MagicMock()
    fake_client.aio.models.generate_content = MagicMock(
        side_effect=Exception("simulated timeout")
    )

    svc = KSpaceAi(provider="gemini", api_key="fake-key", client=fake_client)
    score, feedback = await svc.evaluate_reflection(
        sefira_nombre="Tiféret", texto="x", user_score=5.0,
    )

    # Cayó al stub: score en rango, feedback es el STUB_FEEDBACK
    assert 1.0 <= score <= 10.0
    assert "KSpace-AI no está disponible" in feedback


def _async_return(value):
    async def _coro():
        return value
    return _coro()
```

Y al tope del archivo agregar los nuevos imports (si no están):

```python
import json
from unittest.mock import MagicMock
```

- [ ] **Step 2: Correr para ver que falla**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_llm.py -v
```

Expected: 2 nuevos tests fallan con "unexpected keyword argument 'client'" o similar.

- [ ] **Step 3: Implementar la llamada real**

En `backend/llm.py`, reemplazar el contenido completo por:

```python
"""KSpace-AI: wrapper sobre Gemini para evaluar reflexiones y leer el árbol.

Diseño:
- Una instancia configurada con (provider, api_key). Si provider != "gemini"
  o falta api_key, todas las llamadas devuelven fallbacks deterministas.
- Si el cliente Gemini tira cualquier excepción, también cae al fallback —
  el usuario nunca queda bloqueado por un problema de IA.
"""
from __future__ import annotations

import json
import logging
import random
from typing import Optional


logger = logging.getLogger(__name__)


STUB_FEEDBACK = (
    "KSpace-AI no está disponible en este momento. "
    "Tu reflexión fue guardada y se evaluará cuando vuelva el servicio."
)

EVAL_PROMPT_TEMPLATE = """Sos KSpace-AI, una guía contemplativa basada en la Cábala. Un usuario reflexionó sobre la sefirá "{sefira}" y se autopuntuó {user_score}/10.

Texto del usuario:
\"\"\"
{texto}
\"\"\"

Tarea: leer el texto y devolver en JSON estricto:
- "score": un número 1-10 (puede ser decimal con 1 cifra) reflejando profundidad/coherencia/sinceridad de la reflexión sobre esta sefirá. No es la calidad de la prosa — es qué tanto el texto realmente toca el arquetipo de la sefirá.
- "feedback": 2-3 frases breves en español rioplatense, tono contemplativo, sin moralizar ni dar órdenes. Reflejá lo que ves, sin prescribir.

Devolvé solo el JSON, sin texto antes ni después."""

CAL_READING_PROMPT_TEMPLATE = """Sos KSpace-AI. Estas sefirot del usuario tienen promedio bajo este mes (escala 1-10):

{lista}

Escribí una observación breve (máx 3 frases) en español rioplatense. Tono respetuoso, no moralices ni des órdenes — solo describí lo que ves y mencioná tipos de actividades que tienden a fortalecer cada una. No uses listas con viñetas; que sea un párrafo corrido.

Devolvé solo el texto, sin titulos ni encabezados."""


class KSpaceAi:
    def __init__(self, provider: str, api_key: str, client=None):
        """provider: "stub" o "gemini". client opcional: para inyectar en tests."""
        self.provider = provider
        self.api_key = api_key
        self._client = client

    @property
    def enabled(self) -> bool:
        return self.provider == "gemini" and bool(self.api_key)

    def _get_client(self):
        if self._client is not None:
            return self._client
        # Importación perezosa: solo intentamos importar google-genai si se va
        # a usar de verdad.
        from google import genai
        self._client = genai.Client(api_key=self.api_key)
        return self._client

    # ---- Sync versions (mantenidas para tests sincrónicos) ----

    def evaluate_reflection_sync(
        self, sefira_nombre: str, texto: str, user_score: float,
    ) -> tuple[float, str]:
        return self._stub_evaluate(user_score)

    def generate_calendar_reading_sync(
        self, sefirot_debiles: list[tuple[str, float]],
    ) -> Optional[str]:
        return None

    # ---- Async versions ----

    async def evaluate_reflection(
        self, sefira_nombre: str, texto: str, user_score: float,
    ) -> tuple[float, str]:
        if not self.enabled:
            return self._stub_evaluate(user_score)
        try:
            client = self._get_client()
            prompt = EVAL_PROMPT_TEMPLATE.format(
                sefira=sefira_nombre,
                user_score=round(user_score, 1),
                texto=texto.strip()[:2000],  # límite duro para no escalar tokens
            )
            resp = await client.aio.models.generate_content(
                model="gemini-2.5-flash",
                contents=prompt,
            )
            data = json.loads(resp.text)
            score = float(data["score"])
            score = max(1.0, min(10.0, score))
            feedback = str(data["feedback"]).strip()
            return score, feedback
        except Exception as e:
            logger.warning("KSpaceAi.evaluate_reflection fallback to stub: %s", e)
            return self._stub_evaluate(user_score)

    async def generate_calendar_reading(
        self, sefirot_debiles: list[tuple[str, float]],
    ) -> Optional[str]:
        if not self.enabled:
            return None
        if not sefirot_debiles:
            return None
        try:
            client = self._get_client()
            lista = "\n".join(f"- {nombre}: {score:.1f}" for nombre, score in sefirot_debiles)
            prompt = CAL_READING_PROMPT_TEMPLATE.format(lista=lista)
            resp = await client.aio.models.generate_content(
                model="gemini-2.5-flash",
                contents=prompt,
            )
            return resp.text.strip() or None
        except Exception as e:
            logger.warning("KSpaceAi.generate_calendar_reading fallback to None: %s", e)
            return None

    # ---- Stub helpers ----

    @staticmethod
    def _stub_evaluate(user_score: float) -> tuple[float, str]:
        jitter = random.choice([-1.5, -0.5, 0.0, 0.5, 1.5])
        score = max(1.0, min(10.0, user_score + jitter))
        return score, STUB_FEEDBACK
```

- [ ] **Step 4: Correr todos los tests del módulo**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_llm.py -v
```

Expected: 4 passed.

- [ ] **Step 5: Commit**

```bash
git add backend/llm.py backend/tests/test_llm.py
git commit -m "feat(backend): KSpaceAi.evaluate_reflection llama a Gemini real con fallback stub"
```

---

### Task 6: Implementar `generate_calendar_reading` contra Gemini

**Files:**
- Modify: `backend/tests/test_llm.py`

(La implementación ya quedó hecha en Task 5 — esta tarea es solo agregar tests específicos de la calendar reading para que quede bien cubierta.)

- [ ] **Step 1: Tests adicionales para calendar reading**

Agregar a `backend/tests/test_llm.py`:

```python
@pytest.mark.asyncio
async def test_calendar_reading_calls_gemini_and_returns_text():
    fake_client = MagicMock()
    fake_resp = MagicMock()
    fake_resp.text = "Tu Tiféret está en 4.2 — buscás más equilibrio que del que disponés."
    fake_client.aio.models.generate_content = MagicMock(
        return_value=_async_return(fake_resp)
    )

    svc = KSpaceAi(provider="gemini", api_key="fake-key", client=fake_client)
    text = await svc.generate_calendar_reading([("Tiféret", 4.2), ("Yesod", 3.8)])

    assert text is not None
    assert "Tiféret" in text


@pytest.mark.asyncio
async def test_calendar_reading_empty_input_returns_none():
    """Sin sefirot débiles, no se llama a Gemini."""
    fake_client = MagicMock()
    fake_client.aio.models.generate_content = MagicMock(
        side_effect=AssertionError("no debería llamarse"),
    )
    svc = KSpaceAi(provider="gemini", api_key="fake-key", client=fake_client)
    assert await svc.generate_calendar_reading([]) is None


@pytest.mark.asyncio
async def test_calendar_reading_fallback_on_exception():
    fake_client = MagicMock()
    fake_client.aio.models.generate_content = MagicMock(
        side_effect=Exception("rate limit")
    )
    svc = KSpaceAi(provider="gemini", api_key="fake-key", client=fake_client)
    assert await svc.generate_calendar_reading([("Tiféret", 4.2)]) is None
```

- [ ] **Step 2: Correr todos los tests**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_llm.py -v
```

Expected: 7 passed (los 4 anteriores + estos 3 nuevos).

- [ ] **Step 3: Commit**

```bash
git add backend/tests/test_llm.py
git commit -m "test(backend): cobertura de generate_calendar_reading (success/empty/error)"
```

---

### Task 7: Singleton de `KSpaceAi` + integración en `/evaluate`

**Files:**
- Modify: `backend/main.py`
- Modify: `backend/auth.py`
- Create/modify: `backend/tests/test_evaluate_uses_kspaceai.py`

- [ ] **Step 1: Test failing — /evaluate respeta el toggle del usuario**

Crear `backend/tests/test_evaluate_uses_kspaceai.py`:

```python
"""Test que /evaluate llama a KSpaceAi.evaluate_reflection cuando el usuario
tiene ksai_enabled=True, y cae al stub determinista cuando es False."""
from unittest.mock import patch, AsyncMock

import pytest


@pytest.mark.asyncio
async def test_evaluate_uses_kspaceai_when_user_enabled(
    client, seed_sefirot, two_users,
):
    alice = two_users["alice"]
    with patch("main.kspace_ai.evaluate_reflection", new_callable=AsyncMock) as mock:
        mock.return_value = (7.5, "Buena reflexión.")
        r = await client.post(
            "/evaluate",
            json={"sefira": "Tiféret", "sefira_id": "tiferet", "text": "hola", "score": 6},
            headers=alice["headers"],
        )
    assert r.status_code == 200, r.text
    data = r.json()
    assert data["ai_score"] == 7.5
    assert data["feedback"] == "Buena reflexión."
    mock.assert_awaited_once()


@pytest.mark.asyncio
async def test_evaluate_skips_kspaceai_when_user_disabled(
    client, db_session, seed_sefirot, two_users,
):
    alice = two_users["alice"]
    # Desactivar el toggle directamente en DB
    from models import Usuario
    from sqlalchemy import select
    u = (await db_session.execute(
        select(Usuario).where(Usuario.id == alice["id"])
    )).scalars().first()
    u.ksai_enabled = False
    await db_session.commit()

    with patch("main.kspace_ai.evaluate_reflection", new_callable=AsyncMock) as mock:
        r = await client.post(
            "/evaluate",
            json={"sefira": "Tiféret", "sefira_id": "tiferet", "text": "hola", "score": 6},
            headers=alice["headers"],
        )
    assert r.status_code == 200, r.text
    # El stub determinista NO debería haber llamado al servicio
    mock.assert_not_called()
    # KSpace-AI desactivado: ai_score debe ser null y feedback hardcodeado
    data = r.json()
    assert data["ai_score"] is None
    assert "KSpace-AI desactivado" in data["feedback"]
```

- [ ] **Step 2: Correr para ver que falla**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_evaluate_uses_kspaceai.py -v
```

Expected: AttributeError "module 'main' has no attribute 'kspace_ai'" (o similar).

- [ ] **Step 3: Crear el singleton en main.py**

En `backend/main.py`, después de `settings = get_settings()` y `app = FastAPI()` (alrededor de la línea 48), agregar:

```python
from llm import KSpaceAi
kspace_ai = KSpaceAi(provider=settings.llm_provider, api_key=settings.gemini_api_key)
```

- [ ] **Step 4: Reemplazar el stub de /evaluate**

En `backend/main.py`, reemplazar el cuerpo entero de la función `evaluate` (la que está alrededor de la línea 357-380) por:

```python
@app.post("/evaluate", response_model=EvaluationResponse)
async def evaluate(
    request: EvaluationRequest,
    db: AsyncSession = Depends(get_db),
    user: Usuario = Depends(get_current_user),
):
    if user.ksai_enabled:
        ai_score, feedback = await kspace_ai.evaluate_reflection(
            sefira_nombre=request.sefira,
            texto=request.text,
            user_score=request.score,
        )
    else:
        # KSpace-AI desactivado: no se evalúa con IA, puntuacion_ia queda null
        ai_score = None
        feedback = "KSpace-AI desactivado. Tu reflexión quedó guardada."

    registro = RegistroDiario(
        sefira_id=request.sefira_id,
        reflexion_texto=request.text,
        puntuacion_usuario=int(round(request.score)),
        puntuacion_ia=int(round(ai_score)) if ai_score is not None else None,
        usuario_id=user.id,
    )
    db.add(registro)
    await db.commit()

    return EvaluationResponse(ai_score=ai_score, feedback=feedback)
```

(Removemos el `asyncio.sleep(1)` y el `random.choice(...)`. El delay artificial ya no tiene sentido — el LLM real tiene su propia latencia, y en stub queremos respuesta instantánea.)

- [ ] **Step 5: Agregar `ksai_enabled` a UserOut**

En `backend/auth.py`, dentro de `UserOut`, agregar el campo:

```python
class UserOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: str
    email: EmailStr
    nombre: str
    provider: str = "email"
    ksai_enabled: bool = True
```

- [ ] **Step 6: Correr los tests**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_evaluate_uses_kspaceai.py -v
```

Expected: 2 passed.

- [ ] **Step 7: Commit**

```bash
git add backend/main.py backend/auth.py backend/tests/test_evaluate_uses_kspaceai.py
git commit -m "feat(backend): /evaluate usa KSpaceAi cuando el usuario tiene ksai_enabled"
```

---

### Task 8: Endpoint `PATCH /usuarios/me/ksai`

**Files:**
- Modify: `backend/main.py`
- Create: `backend/tests/test_ksai_endpoints.py`

- [ ] **Step 1: Test failing — toggle on/off del flag**

Crear `backend/tests/test_ksai_endpoints.py`:

```python
"""Tests integración para los endpoints PATCH /usuarios/me/ksai y /ia/calendario/*."""
import pytest


@pytest.mark.asyncio
async def test_patch_ksai_toggles_flag(client, seed_sefirot, two_users):
    alice = two_users["alice"]

    # Default: enabled
    r = await client.get("/auth/me", headers=alice["headers"])
    assert r.status_code == 200
    assert r.json()["ksai_enabled"] is True

    # Desactivar
    r = await client.patch(
        "/usuarios/me/ksai",
        json={"enabled": False},
        headers=alice["headers"],
    )
    assert r.status_code == 200
    assert r.json()["ksai_enabled"] is False

    # /auth/me lo refleja
    r = await client.get("/auth/me", headers=alice["headers"])
    assert r.json()["ksai_enabled"] is False

    # Reactivar
    r = await client.patch(
        "/usuarios/me/ksai",
        json={"enabled": True},
        headers=alice["headers"],
    )
    assert r.status_code == 200
    assert r.json()["ksai_enabled"] is True


@pytest.mark.asyncio
async def test_patch_ksai_requires_auth(client):
    r = await client.patch("/usuarios/me/ksai", json={"enabled": False})
    assert r.status_code == 401
```

- [ ] **Step 2: Correr para ver que falla**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_ksai_endpoints.py -v
```

Expected: 404 "Not Found" en ambos tests.

- [ ] **Step 3: Implementar el endpoint**

En `backend/main.py`, agregar (ubicación: justo después del endpoint `/auth/me`, alrededor de la línea 112):

```python
class KsaiToggleRequest(BaseModel):
    enabled: bool


@app.patch("/usuarios/me/ksai", response_model=UserOut)
async def patch_ksai_toggle(
    payload: KsaiToggleRequest,
    db: AsyncSession = Depends(get_db),
    user: Usuario = Depends(get_current_user),
):
    user.ksai_enabled = payload.enabled
    await db.commit()
    await db.refresh(user)
    return user
```

- [ ] **Step 4: Correr los tests**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_ksai_endpoints.py -v
```

Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add backend/main.py backend/tests/test_ksai_endpoints.py
git commit -m "feat(backend): PATCH /usuarios/me/ksai toggle de opt-in"
```

---

### Task 9: Endpoint `GET /ia/calendario/lectura`

**Files:**
- Modify: `backend/main.py`
- Modify: `backend/tests/test_ksai_endpoints.py`

- [ ] **Step 1: Tests failing — los 4 statuses + el filtrado por mes**

Agregar a `backend/tests/test_ksai_endpoints.py`:

```python
from datetime import datetime, timezone
from sqlalchemy import select
from unittest.mock import patch, AsyncMock


@pytest.mark.asyncio
async def test_lectura_status_no_data_sin_reflexiones(client, seed_sefirot, two_users):
    """Usuario sin reflexiones este mes → no_data."""
    alice = two_users["alice"]
    r = await client.get("/ia/calendario/lectura", headers=alice["headers"])
    assert r.status_code == 200
    body = r.json()
    assert body["status"] == "no_data"
    assert body["weak_sefirot"] == []
    assert "Aún sin reflexiones" in body["message"]


@pytest.mark.asyncio
async def test_lectura_status_disabled_si_toggle_off(
    client, db_session, seed_sefirot, two_users,
):
    alice = two_users["alice"]
    from models import Usuario
    u = (await db_session.execute(
        select(Usuario).where(Usuario.id == alice["id"])
    )).scalars().first()
    u.ksai_enabled = False
    await db_session.commit()

    r = await client.get("/ia/calendario/lectura", headers=alice["headers"])
    assert r.status_code == 200
    body = r.json()
    assert body["status"] == "disabled"
    assert body["weak_sefirot"] == []
    assert "Activá KSpace-AI" in body["message"]


@pytest.mark.asyncio
async def test_lectura_status_balanced_si_promedio_alto(
    client, db_session, seed_sefirot, two_users,
):
    """Reflexión este mes con scores altos → balanced."""
    alice = two_users["alice"]
    from models import RegistroDiario
    now = datetime.utcnow()
    db_session.add(RegistroDiario(
        sefira_id="tiferet",
        reflexion_texto="x",
        puntuacion_usuario=8,
        puntuacion_ia=8,
        usuario_id=alice["id"],
        fecha_registro=now,
    ))
    await db_session.commit()

    r = await client.get("/ia/calendario/lectura", headers=alice["headers"])
    body = r.json()
    assert body["status"] == "balanced"
    assert body["weak_sefirot"] == []
    assert "balanceado" in body["message"]


@pytest.mark.asyncio
async def test_lectura_status_weak_llama_a_kspace_ai(
    client, db_session, seed_sefirot, two_users,
):
    """Una sefirá con promedio < 5 → status=weak, llama al LLM."""
    alice = two_users["alice"]
    from models import RegistroDiario
    now = datetime.utcnow()
    # tiferet promedio 3.5 (user=3, ia=4)
    db_session.add(RegistroDiario(
        sefira_id="tiferet",
        reflexion_texto="x",
        puntuacion_usuario=3,
        puntuacion_ia=4,
        usuario_id=alice["id"],
        fecha_registro=now,
    ))
    await db_session.commit()

    with patch("main.kspace_ai.generate_calendar_reading", new_callable=AsyncMock) as mock:
        mock.return_value = "Tu Tiféret pide equilibrio."
        r = await client.get("/ia/calendario/lectura", headers=alice["headers"])
    assert r.status_code == 200
    body = r.json()
    assert body["status"] == "weak"
    assert len(body["weak_sefirot"]) == 1
    assert body["weak_sefirot"][0]["id"] == "tiferet"
    assert body["weak_sefirot"][0]["score"] == 3.5
    assert body["message"] == "Tu Tiféret pide equilibrio."
    mock.assert_awaited_once_with([("Tiféret", 3.5)])


@pytest.mark.asyncio
async def test_lectura_status_weak_message_null_si_llm_falla(
    client, db_session, seed_sefirot, two_users,
):
    alice = two_users["alice"]
    from models import RegistroDiario
    db_session.add(RegistroDiario(
        sefira_id="tiferet", reflexion_texto="x",
        puntuacion_usuario=3, puntuacion_ia=4,
        usuario_id=alice["id"], fecha_registro=datetime.utcnow(),
    ))
    await db_session.commit()

    with patch("main.kspace_ai.generate_calendar_reading", new_callable=AsyncMock) as mock:
        mock.return_value = None
        r = await client.get("/ia/calendario/lectura", headers=alice["headers"])
    body = r.json()
    assert body["status"] == "weak"
    assert body["message"] is None
    assert len(body["weak_sefirot"]) == 1


@pytest.mark.asyncio
async def test_lectura_aisla_por_usuario(client, db_session, seed_sefirot, two_users):
    """Reflexiones de Bob no afectan la lectura de Alice."""
    alice = two_users["alice"]
    bob = two_users["bob"]
    from models import RegistroDiario
    # Bob tiene una sefirá floja
    db_session.add(RegistroDiario(
        sefira_id="tiferet", reflexion_texto="x",
        puntuacion_usuario=2, puntuacion_ia=2,
        usuario_id=bob["id"], fecha_registro=datetime.utcnow(),
    ))
    await db_session.commit()

    r = await client.get("/ia/calendario/lectura", headers=alice["headers"])
    body = r.json()
    # Alice no debería ver nada (no tiene data propia)
    assert body["status"] == "no_data"
```

- [ ] **Step 2: Correr para ver que fallan**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_ksai_endpoints.py -v -k "lectura"
```

Expected: 6 fallos 404.

- [ ] **Step 3: Implementar el endpoint**

En `backend/main.py`, agregar (ubicación sugerida: cerca de los endpoints de `/espejo/evolucion`, después de la función `espejo_evolucion_semanas`):

```python
class WeakSefiraOut(BaseModel):
    id: str
    nombre: str
    score: float


class LecturaResponse(BaseModel):
    status: str  # "weak" | "balanced" | "no_data" | "disabled"
    weak_sefirot: list[WeakSefiraOut]
    message: Optional[str] = None


@app.get("/ia/calendario/lectura", response_model=LecturaResponse)
async def ia_calendario_lectura(
    db: AsyncSession = Depends(get_db),
    user: Usuario = Depends(get_current_user),
):
    if not user.ksai_enabled:
        return LecturaResponse(
            status="disabled",
            weak_sefirot=[],
            message="Activá KSpace-AI en tu perfil para ver la lectura mensual.",
        )

    # Definir el rango del mes corriente (UTC).
    now = datetime.utcnow()
    mes_inicio = datetime(now.year, now.month, 1)
    if now.month == 12:
        mes_fin = datetime(now.year + 1, 1, 1)
    else:
        mes_fin = datetime(now.year, now.month + 1, 1)

    sefirot = (await db.execute(select(Sefira).order_by(Sefira.nombre))).scalars().all()
    regs = (await db.execute(
        select(RegistroDiario).where(
            RegistroDiario.usuario_id == user.id,
            RegistroDiario.fecha_registro >= mes_inicio,
            RegistroDiario.fecha_registro < mes_fin,
        )
    )).scalars().all()

    if not regs:
        return LecturaResponse(
            status="no_data",
            weak_sefirot=[],
            message="Aún sin reflexiones este mes. Cuando reflexiones, KSpace-AI empezará a leer tu árbol.",
        )

    # Promedio por sefirá: (user + ia) / 2 sobre las reflexiones del mes
    promedio_por_sefira: dict[str, float] = {}
    for s in sefirot:
        s_regs = [r for r in regs if r.sefira_id == s.id]
        if not s_regs:
            continue
        vals = []
        for r in s_regs:
            u_score = r.puntuacion_usuario
            i_score = r.puntuacion_ia
            if u_score is None and i_score is None:
                continue
            if u_score is None:
                vals.append(float(i_score))
            elif i_score is None:
                vals.append(float(u_score))
            else:
                vals.append((float(u_score) + float(i_score)) / 2.0)
        if vals:
            promedio_por_sefira[s.id] = sum(vals) / len(vals)

    weak: list[WeakSefiraOut] = []
    sefira_by_id = {s.id: s for s in sefirot}
    for sefira_id, prom in sorted(promedio_por_sefira.items(), key=lambda kv: kv[1]):
        if prom < 5.0:
            weak.append(WeakSefiraOut(
                id=sefira_id,
                nombre=sefira_by_id[sefira_id].nombre,
                score=round(prom, 1),
            ))

    if not weak:
        return LecturaResponse(
            status="balanced",
            weak_sefirot=[],
            message="Tu árbol está balanceado este mes.",
        )

    pairs = [(w.nombre, w.score) for w in weak]
    message = await kspace_ai.generate_calendar_reading(pairs)
    return LecturaResponse(status="weak", weak_sefirot=weak, message=message)
```

- [ ] **Step 4: Correr todos los tests del archivo**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_ksai_endpoints.py -v
```

Expected: 8 passed (los 2 del PATCH + 6 de lectura).

- [ ] **Step 5: Commit**

```bash
git add backend/main.py backend/tests/test_ksai_endpoints.py
git commit -m "feat(backend): GET /ia/calendario/lectura con 4 statuses + filtrado por mes"
```

---

### Task 10: Endpoint `POST /ia/calendario/felicitacion`

**Files:**
- Modify: `backend/main.py`
- Modify: `backend/tests/test_ksai_endpoints.py`

- [ ] **Step 1: Tests failing — felicitación solo si la sefirá es floja**

Agregar a `backend/tests/test_ksai_endpoints.py`:

```python
@pytest.mark.asyncio
async def test_felicitacion_no_show_si_sefira_no_floja(
    client, db_session, seed_sefirot, two_users,
):
    """Sefirá con promedio >= 5 no dispara felicitación."""
    alice = two_users["alice"]
    from models import RegistroDiario, Actividad, ActividadSefira
    from datetime import datetime as dt

    db_session.add(RegistroDiario(
        sefira_id="tiferet", reflexion_texto="x",
        puntuacion_usuario=8, puntuacion_ia=8,
        usuario_id=alice["id"], fecha_registro=dt.utcnow(),
    ))
    act = Actividad(
        titulo="Meditar",
        inicio=dt.utcnow(),
        fin=dt.utcnow(),
        usuario_id=alice["id"],
    )
    db_session.add(act)
    await db_session.flush()
    db_session.add(ActividadSefira(actividad_id=act.id, sefira_id="tiferet"))
    await db_session.commit()

    r = await client.post(
        "/ia/calendario/felicitacion",
        json={"actividad_id": act.id},
        headers=alice["headers"],
    )
    assert r.status_code == 200
    assert r.json()["show"] is False


@pytest.mark.asyncio
async def test_felicitacion_show_si_sefira_floja_singular(
    client, db_session, seed_sefirot, two_users,
):
    """Sefirá con promedio < 5 + 1 actividad del mes → felicitación con 'actividad' singular."""
    alice = two_users["alice"]
    from models import RegistroDiario, Actividad, ActividadSefira
    from datetime import datetime as dt
    now = dt.utcnow()

    db_session.add(RegistroDiario(
        sefira_id="tiferet", reflexion_texto="x",
        puntuacion_usuario=2, puntuacion_ia=3,
        usuario_id=alice["id"], fecha_registro=now,
    ))
    act = Actividad(
        titulo="Meditar",
        inicio=now, fin=now,
        usuario_id=alice["id"],
    )
    db_session.add(act)
    await db_session.flush()
    db_session.add(ActividadSefira(actividad_id=act.id, sefira_id="tiferet"))
    await db_session.commit()

    r = await client.post(
        "/ia/calendario/felicitacion",
        json={"actividad_id": act.id},
        headers=alice["headers"],
    )
    body = r.json()
    assert body["show"] is True
    assert body["sefira_nombre"] == "Tiféret"
    assert body["count"] == 1
    assert body["message"] == "Bien, agregaste 1 actividad a tu Tiféret. Te lo agradecerá."


@pytest.mark.asyncio
async def test_felicitacion_show_plural(
    client, db_session, seed_sefirot, two_users,
):
    """Tres actividades del mes en Tiféret floja → plural."""
    alice = two_users["alice"]
    from models import RegistroDiario, Actividad, ActividadSefira
    from datetime import datetime as dt
    now = dt.utcnow()

    db_session.add(RegistroDiario(
        sefira_id="tiferet", reflexion_texto="x",
        puntuacion_usuario=2, puntuacion_ia=3,
        usuario_id=alice["id"], fecha_registro=now,
    ))
    last_id = None
    for i in range(3):
        a = Actividad(titulo=f"act{i}", inicio=now, fin=now, usuario_id=alice["id"])
        db_session.add(a)
        await db_session.flush()
        db_session.add(ActividadSefira(actividad_id=a.id, sefira_id="tiferet"))
        last_id = a.id
    await db_session.commit()

    r = await client.post(
        "/ia/calendario/felicitacion",
        json={"actividad_id": last_id},
        headers=alice["headers"],
    )
    body = r.json()
    assert body["show"] is True
    assert body["count"] == 3
    assert body["message"] == "Bien, agregaste 3 actividades a tu Tiféret. Te lo agradecerá."


@pytest.mark.asyncio
async def test_felicitacion_elige_sefira_mas_floja(
    client, db_session, seed_sefirot, two_users,
):
    """Actividad taggeada a 2 sefirot flojas → elige la de promedio menor."""
    alice = two_users["alice"]
    from models import RegistroDiario, Actividad, ActividadSefira
    from datetime import datetime as dt
    now = dt.utcnow()

    db_session.add(RegistroDiario(
        sefira_id="tiferet", reflexion_texto="x",
        puntuacion_usuario=3, puntuacion_ia=3,
        usuario_id=alice["id"], fecha_registro=now,
    ))  # tiferet promedio 3.0
    db_session.add(RegistroDiario(
        sefira_id="jesed", reflexion_texto="x",
        puntuacion_usuario=4, puntuacion_ia=4,
        usuario_id=alice["id"], fecha_registro=now,
    ))  # jesed promedio 4.0
    act = Actividad(titulo="x", inicio=now, fin=now, usuario_id=alice["id"])
    db_session.add(act)
    await db_session.flush()
    db_session.add(ActividadSefira(actividad_id=act.id, sefira_id="tiferet"))
    db_session.add(ActividadSefira(actividad_id=act.id, sefira_id="jesed"))
    await db_session.commit()

    r = await client.post(
        "/ia/calendario/felicitacion",
        json={"actividad_id": act.id},
        headers=alice["headers"],
    )
    body = r.json()
    assert body["show"] is True
    assert body["sefira_nombre"] == "Tiféret"  # 3.0 < 4.0


@pytest.mark.asyncio
async def test_felicitacion_404_actividad_inexistente(client, two_users):
    alice = two_users["alice"]
    r = await client.post(
        "/ia/calendario/felicitacion",
        json={"actividad_id": "no-existe"},
        headers=alice["headers"],
    )
    assert r.status_code == 404


@pytest.mark.asyncio
async def test_felicitacion_404_actividad_de_otro_usuario(
    client, db_session, seed_sefirot, two_users,
):
    alice = two_users["alice"]
    bob = two_users["bob"]
    from models import Actividad
    from datetime import datetime as dt
    act = Actividad(titulo="x", inicio=dt.utcnow(), fin=dt.utcnow(), usuario_id=bob["id"])
    db_session.add(act)
    await db_session.commit()

    r = await client.post(
        "/ia/calendario/felicitacion",
        json={"actividad_id": act.id},
        headers=alice["headers"],
    )
    assert r.status_code == 404
```

- [ ] **Step 2: Correr para ver que fallan**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_ksai_endpoints.py -v -k "felicitacion"
```

Expected: 6 fallos 404.

- [ ] **Step 3: Implementar el endpoint**

En `backend/main.py`, después del endpoint `/ia/calendario/lectura` agregar:

```python
class FelicitacionRequest(BaseModel):
    actividad_id: str


class FelicitacionResponse(BaseModel):
    show: bool
    sefira_nombre: Optional[str] = None
    count: Optional[int] = None
    message: Optional[str] = None


@app.post("/ia/calendario/felicitacion", response_model=FelicitacionResponse)
async def ia_calendario_felicitacion(
    payload: FelicitacionRequest,
    db: AsyncSession = Depends(get_db),
    user: Usuario = Depends(get_current_user),
):
    # 1. Buscar la actividad y verificar ownership
    act = (await db.execute(
        select(Actividad).where(
            Actividad.id == payload.actividad_id,
            Actividad.usuario_id == user.id,
        )
    )).scalars().first()
    if act is None:
        raise HTTPException(status_code=404, detail="Actividad no encontrada")

    # 2. Sefirot taggeadas
    sefirot_ids = (await db.execute(
        select(ActividadSefira.sefira_id).where(ActividadSefira.actividad_id == act.id)
    )).scalars().all()
    if not sefirot_ids:
        return FelicitacionResponse(show=False)

    # 3. Rango del mes corriente (UTC)
    now = datetime.utcnow()
    mes_inicio = datetime(now.year, now.month, 1)
    if now.month == 12:
        mes_fin = datetime(now.year + 1, 1, 1)
    else:
        mes_fin = datetime(now.year, now.month + 1, 1)

    # 4. Promedio por sefirá taggeada → quedarse con la más floja con promedio < 5
    regs = (await db.execute(
        select(RegistroDiario).where(
            RegistroDiario.usuario_id == user.id,
            RegistroDiario.sefira_id.in_(sefirot_ids),
            RegistroDiario.fecha_registro >= mes_inicio,
            RegistroDiario.fecha_registro < mes_fin,
        )
    )).scalars().all()

    promedios: dict[str, float] = {}
    for sid in sefirot_ids:
        s_regs = [r for r in regs if r.sefira_id == sid]
        if not s_regs:
            continue
        vals = []
        for r in s_regs:
            u_s, i_s = r.puntuacion_usuario, r.puntuacion_ia
            if u_s is None and i_s is None:
                continue
            if u_s is None:
                vals.append(float(i_s))
            elif i_s is None:
                vals.append(float(u_s))
            else:
                vals.append((float(u_s) + float(i_s)) / 2.0)
        if vals:
            promedios[sid] = sum(vals) / len(vals)

    floja = None  # (sefira_id, promedio)
    for sid, prom in promedios.items():
        if prom < 5.0 and (floja is None or prom < floja[1]):
            floja = (sid, prom)

    if floja is None:
        return FelicitacionResponse(show=False)

    # 5. Contar actividades del mes para la sefirá elegida
    count = (await db.execute(
        select(func.count())
        .select_from(Actividad)
        .join(ActividadSefira, ActividadSefira.actividad_id == Actividad.id)
        .where(
            Actividad.usuario_id == user.id,
            ActividadSefira.sefira_id == floja[0],
            Actividad.inicio >= mes_inicio,
            Actividad.inicio < mes_fin,
        )
    )).scalar_one()

    # 6. Nombre de la sefirá + template
    sefira_obj = (await db.execute(
        select(Sefira).where(Sefira.id == floja[0])
    )).scalars().first()
    nombre = sefira_obj.nombre if sefira_obj else floja[0]

    actividad_palabra = "actividad" if count == 1 else "actividades"
    message = f"Bien, agregaste {count} {actividad_palabra} a tu {nombre}. Te lo agradecerá."

    return FelicitacionResponse(
        show=True,
        sefira_nombre=nombre,
        count=count,
        message=message,
    )
```

- [ ] **Step 4: Correr los tests**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest tests/test_ksai_endpoints.py -v
```

Expected: 14 passed (los 8 anteriores + 6 nuevos).

- [ ] **Step 5: Commit**

```bash
git add backend/main.py backend/tests/test_ksai_endpoints.py
git commit -m "feat(backend): POST /ia/calendario/felicitacion con template singular/plural"
```

---

### Task 11: Frontend — tipos + hook `useIaLectura`

**Files:**
- Create: `frontend/src/calendar/hooks/useIaLectura.ts`
- Modify: `frontend/src/auth/types.ts` (extender User con `ksai_enabled`)
- Modify: `frontend/src/auth/AuthContext.tsx` (lo refleja al hidratar)

- [ ] **Step 1: Extender el tipo User**

En `frontend/src/auth/types.ts`, en la interface `User` agregar el campo:

```typescript
export interface User {
  id: string;
  email: string;
  nombre: string;
  provider: AuthProvider;
  ksai_enabled: boolean;
}
```

- [ ] **Step 2: Asegurar que AuthContext propaga el campo**

En `frontend/src/auth/AuthContext.tsx`, donde se hidrata el `User` desde la respuesta del backend (buscar `setUser(`), verificar que el objeto que se pasa incluye `ksai_enabled` del response. Si está mapeando campo por campo, agregar la línea. Si está spread con `...data`, no hace falta.

Run para verificar:

```bash
grep -n "setUser\|ksai_enabled" frontend/src/auth/AuthContext.tsx
```

Si la asignación es por campos individuales, modificarla a:

```typescript
setUser({
  id: data.id,
  email: data.email,
  nombre: data.nombre,
  provider: data.provider,
  ksai_enabled: data.ksai_enabled ?? true,
});
```

- [ ] **Step 3: Crear el hook**

Crear `frontend/src/calendar/hooks/useIaLectura.ts`:

```typescript
import { useEffect, useState, useCallback } from 'react';
import { apiFetch, useAuth } from '../../auth';

export type LecturaStatus = 'weak' | 'balanced' | 'no_data' | 'disabled';

export type WeakSefira = {
  id: string;
  nombre: string;
  score: number;
};

export type LecturaResponse = {
  status: LecturaStatus;
  weak_sefirot: WeakSefira[];
  message: string | null;
};

/**
 * Fetch de la lectura mensual de KSpace-AI para el Calendario.
 * Re-fetcha cuando `dependency` cambia (ej. cuando se crea una actividad y
 * cambia el estado de una sefirá floja).
 */
export function useIaLectura(dependency?: unknown) {
  const { status } = useAuth();
  const [data, setData] = useState<LecturaResponse | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string>('');

  const reload = useCallback(async () => {
    if (status !== 'authenticated') {
      setData(null);
      setError('');
      return;
    }
    setLoading(true);
    setError('');
    try {
      const res = await apiFetch('/ia/calendario/lectura');
      if (!res.ok) throw new Error('No se pudo cargar la lectura');
      setData(await res.json());
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Error');
    } finally {
      setLoading(false);
    }
  }, [status]);

  // eslint-disable-next-line react-hooks/exhaustive-deps
  useEffect(() => { void reload(); }, [reload, dependency]);

  return { data, loading, error, reload };
}
```

- [ ] **Step 4: Type-check**

Run desde `frontend/`:

```bash
npx tsc --noEmit
```

Expected: exit 0.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/auth/types.ts frontend/src/auth/AuthContext.tsx frontend/src/calendar/hooks/useIaLectura.ts
git commit -m "feat(frontend): tipo User.ksai_enabled + hook useIaLectura"
```

---

### Task 12: Frontend — componente `<CalendarioIaLectura />`

**Files:**
- Create: `frontend/src/calendar/components/CalendarioIaLectura.tsx`

- [ ] **Step 1: Crear el componente**

Crear `frontend/src/calendar/components/CalendarioIaLectura.tsx`:

```typescript
import { useEffect, useState } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { X } from 'lucide-react';
import { useIaLectura } from '../hooks/useIaLectura';
import { SEFIRA_COLORS } from '../../shared/tokens';

/** Key en sessionStorage para "el usuario cerró la card hoy". */
const DISMISS_KEY_PREFIX = 'kspaceai-lectura-dismissed:';

function todayKey(): string {
  const d = new Date();
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
}

type Props = {
  /** Cambia el valor para forzar refetch (ej. al crear una actividad). */
  refreshKey?: unknown;
};

export default function CalendarioIaLectura({ refreshKey }: Props) {
  const { data, loading, error } = useIaLectura(refreshKey);
  const [dismissed, setDismissed] = useState<boolean>(() => {
    if (typeof window === 'undefined') return false;
    return sessionStorage.getItem(DISMISS_KEY_PREFIX + todayKey()) === '1';
  });

  // Reset el dismissed si cambia el día (sesión larga cruzando medianoche)
  useEffect(() => {
    const check = () => {
      const flag = sessionStorage.getItem(DISMISS_KEY_PREFIX + todayKey()) === '1';
      setDismissed(flag);
    };
    const t = window.setInterval(check, 60000);
    return () => window.clearInterval(t);
  }, []);

  function handleDismiss() {
    sessionStorage.setItem(DISMISS_KEY_PREFIX + todayKey(), '1');
    setDismissed(true);
  }

  if (dismissed) return null;
  if (loading && !data) {
    // Skeleton sutil
    return (
      <div className="w-full rounded-xl bg-stone-900/30 border border-stone-800/40 px-5 py-4 mb-4">
        <div className="h-3 w-24 rounded bg-stone-800/60 animate-pulse mb-2" />
        <div className="h-4 w-2/3 rounded bg-stone-800/40 animate-pulse" />
      </div>
    );
  }
  if (error || !data) return null;

  // Render variantes por status
  return (
    <AnimatePresence>
      <motion.div
        initial={{ opacity: 0, y: -8 }}
        animate={{ opacity: 1, y: 0 }}
        exit={{ opacity: 0, y: -8 }}
        transition={{ duration: 0.3, ease: [0.16, 1, 0.3, 1] }}
        className="relative w-full rounded-xl bg-gradient-to-br from-stone-900/60 to-stone-950/40 border border-stone-700/40 px-5 py-4 mb-4"
      >
        <button
          type="button"
          onClick={handleDismiss}
          aria-label="Cerrar lectura"
          className="absolute top-3 right-3 text-stone-500 hover:text-stone-200 transition-colors"
        >
          <X size={16} />
        </button>

        <div className="text-[10px] uppercase tracking-[0.16em] text-amber-200/80 mb-2">
          ✦ Lectura del mes
        </div>

        {data.status === 'weak' && (
          <>
            {data.message && (
              <p className="text-stone-200 text-sm leading-relaxed font-serif mb-3 pr-6">
                {data.message}
              </p>
            )}
            <div className="flex flex-wrap gap-2">
              {data.weak_sefirot.map(s => (
                <span
                  key={s.id}
                  className="inline-flex items-center gap-1.5 text-[10px] uppercase tracking-[0.12em] px-2.5 py-1 rounded-full border"
                  style={{
                    background: `${SEFIRA_COLORS[s.id] ?? '#a3a3a3'}22`,
                    borderColor: `${SEFIRA_COLORS[s.id] ?? '#a3a3a3'}55`,
                    color: '#f5f5f5',
                  }}
                >
                  <span className="w-1.5 h-1.5 rounded-full" style={{ background: SEFIRA_COLORS[s.id] ?? '#a3a3a3' }} />
                  {s.nombre} {s.score.toFixed(1)}
                </span>
              ))}
            </div>
          </>
        )}

        {data.status === 'balanced' && (
          <p className="text-stone-300 text-sm leading-relaxed font-serif pr-6">
            {data.message}
          </p>
        )}

        {data.status === 'no_data' && (
          <p className="text-stone-400 text-sm leading-relaxed font-serif italic pr-6">
            {data.message}
          </p>
        )}

        {data.status === 'disabled' && (
          <p className="text-stone-400 text-sm leading-relaxed font-serif pr-6">
            {data.message}
          </p>
        )}
      </motion.div>
    </AnimatePresence>
  );
}
```

- [ ] **Step 2: Montar el componente en el Calendario**

Abrir `frontend/src/calendar/CalendarModule.tsx` y agregar import:

```typescript
import CalendarioIaLectura from './components/CalendarioIaLectura';
```

Buscar el lugar donde se renderiza el header del calendario (donde aparece "Mayo", "SEMANA DEL ..."). Inmediatamente DESPUÉS del header y ANTES del grid del calendario, insertar:

```tsx
<CalendarioIaLectura refreshKey={undefined} />
```

(En la próxima task, `refreshKey` se conecta a un contador que se incrementa al crear una actividad — eso fuerza el re-fetch de la lectura.)

- [ ] **Step 3: Type-check + verificar visualmente**

Run desde `frontend/`:

```bash
npx tsc --noEmit
```

Expected: exit 0.

Abrir el navegador en localhost:5173, entrar al Calendario logueado. Debería verse la card según el status del usuario actual (`no_data` para un user nuevo, `weak/balanced` si ya tiene reflexiones del mes).

- [ ] **Step 4: Commit**

```bash
git add frontend/src/calendar/components/CalendarioIaLectura.tsx frontend/src/calendar/CalendarModule.tsx
git commit -m "feat(frontend): CalendarioIaLectura card al tope del Calendario"
```

---

### Task 13: Frontend — toast de felicitación + integración con creación de actividad

**Files:**
- Create: `frontend/src/calendar/components/FelicitacionToast.tsx`
- Create: `frontend/src/calendar/hooks/useFelicitacion.ts`
- Modify: `frontend/src/calendar/components/ActivityForm.tsx`
- Modify: `frontend/src/calendar/CalendarModule.tsx`

- [ ] **Step 1: Hook que llama al endpoint**

Crear `frontend/src/calendar/hooks/useFelicitacion.ts`:

```typescript
import { useState, useCallback } from 'react';
import { apiFetch } from '../../auth';

export type Felicitacion = {
  sefira_id?: string;  // no viene del backend, lo seteamos local si lo necesitamos
  sefira_nombre: string;
  count: number;
  message: string;
};

/**
 * Tras crear una actividad, llama POST /ia/calendario/felicitacion.
 * Si el backend dice {show:true}, expone la felicitacion para que un
 * <FelicitacionToast> la muestre. Auto-clear después de 4 segundos.
 */
export function useFelicitacion() {
  const [felicitacion, setFelicitacion] = useState<Felicitacion | null>(null);

  const trigger = useCallback(async (actividadId: string) => {
    try {
      const res = await apiFetch('/ia/calendario/felicitacion', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ actividad_id: actividadId }),
      });
      if (!res.ok) return;
      const data = await res.json();
      if (data.show) {
        setFelicitacion({
          sefira_nombre: data.sefira_nombre,
          count: data.count,
          message: data.message,
        });
        window.setTimeout(() => setFelicitacion(null), 4000);
      }
    } catch {
      // Silencioso: la felicitación es accesorio
    }
  }, []);

  function dismiss() { setFelicitacion(null); }

  return { felicitacion, trigger, dismiss };
}
```

- [ ] **Step 2: Componente del toast**

Crear `frontend/src/calendar/components/FelicitacionToast.tsx`:

```typescript
import { AnimatePresence, motion } from 'framer-motion';
import { Sparkles, X } from 'lucide-react';
import type { Felicitacion } from '../hooks/useFelicitacion';
import { SEFIRA_COLORS } from '../../shared/tokens';

type Props = {
  felicitacion: Felicitacion | null;
  onDismiss: () => void;
};

export default function FelicitacionToast({ felicitacion, onDismiss }: Props) {
  const color = felicitacion
    ? (Object.entries(SEFIRA_COLORS).find(([_, _v]) => false)?.[1] ?? '#e9c349')
    : '#e9c349';
  // Buscar el color por nombre normalizado (el backend manda nombre, no id)
  const matched = felicitacion
    ? Object.entries(SEFIRA_COLORS).find(([id]) => id.toLowerCase().startsWith(
        felicitacion.sefira_nombre.toLowerCase().slice(0, 4)
      ))
    : null;
  const borderColor = matched ? matched[1] : color;

  return (
    <AnimatePresence>
      {felicitacion && (
        <motion.div
          key={felicitacion.sefira_nombre + felicitacion.count}
          initial={{ opacity: 0, y: -16, scale: 0.96 }}
          animate={{ opacity: 1, y: 0, scale: 1 }}
          exit={{ opacity: 0, y: -16, scale: 0.96 }}
          transition={{ duration: 0.3, ease: [0.16, 1, 0.3, 1] }}
          className="fixed top-20 right-6 z-50 max-w-sm rounded-xl bg-stone-950/95 backdrop-blur-xl border border-stone-700/50 shadow-2xl pl-4 pr-3 py-3 flex items-start gap-3"
          style={{ borderLeftColor: borderColor, borderLeftWidth: 3 }}
        >
          <Sparkles size={16} className="text-amber-200 mt-0.5 shrink-0" />
          <p className="text-sm text-stone-200 leading-snug font-serif pr-2">
            {felicitacion.message}
          </p>
          <button
            type="button"
            onClick={onDismiss}
            aria-label="Cerrar"
            className="text-stone-500 hover:text-stone-200 transition-colors shrink-0"
          >
            <X size={14} />
          </button>
        </motion.div>
      )}
    </AnimatePresence>
  );
}
```

- [ ] **Step 3: Disparar la felicitación desde ActivityForm**

En `frontend/src/calendar/components/ActivityForm.tsx`, buscar el lugar donde se procesa la respuesta exitosa del POST `/actividades` (status 201, devuelve una lista con la(s) actividad(es) creada(s)). Identificar cómo se llama al callback `onSaved` o equivalente.

Modificar el componente para que reciba una nueva prop:

```typescript
type Props = {
  // ... props existentes
  onActividadCreada?: (actividadId: string) => void;
};
```

En la lógica de `handleSubmit` (o equivalente), después de que el POST `/actividades` retorna 201 con el array `created`, agregar (solo para la **primera** actividad creada, así no spammea con recurrentes):

```typescript
if (created.length > 0 && onActividadCreada) {
  onActividadCreada(created[0].id);
}
```

- [ ] **Step 4: Pasar la felicitación al CalendarModule**

En `frontend/src/calendar/CalendarModule.tsx`:

```typescript
import { useState } from 'react';
import { useFelicitacion } from './hooks/useFelicitacion';
import FelicitacionToast from './components/FelicitacionToast';

// dentro del componente:
const { felicitacion, trigger, dismiss } = useFelicitacion();
const [actividadCreadaCount, setActividadCreadaCount] = useState(0);

function handleActividadCreada(actividadId: string) {
  void trigger(actividadId);
  setActividadCreadaCount(c => c + 1);
}
```

En el JSX, donde se renderiza `<CalendarioIaLectura />`, cambiar a:

```tsx
<CalendarioIaLectura refreshKey={actividadCreadaCount} />
```

(Esto fuerza refetch de la lectura cuando se crea una actividad — útil porque puede haber cambiado el conteo de actividades para una sefirá floja.)

Y en el lugar donde se renderiza el `<ActivityForm />`, pasarle:

```tsx
<ActivityForm
  // ... props existentes
  onActividadCreada={handleActividadCreada}
/>
```

Y al final del JSX del CalendarModule, agregar el toast:

```tsx
<FelicitacionToast felicitacion={felicitacion} onDismiss={dismiss} />
```

- [ ] **Step 5: Type-check + verificar end-to-end**

Run desde `frontend/`:

```bash
npx tsc --noEmit
```

Expected: exit 0.

Verificar en el navegador:
1. Crear una actividad taggeada a una sefirá que tengas con promedio < 5 este mes.
2. Después de guardar, debería aparecer un toast top-right "Bien, agregaste X actividades a tu {sefira}. Te lo agradecerá."
3. Si la sefirá no está floja, no aparece nada.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/calendar/hooks/useFelicitacion.ts frontend/src/calendar/components/FelicitacionToast.tsx frontend/src/calendar/components/ActivityForm.tsx frontend/src/calendar/CalendarModule.tsx
git commit -m "feat(frontend): toast de felicitacion al agregar actividad de sefira floja"
```

---

### Task 14: Toggle KSpace-AI en el UserMenu

**Files:**
- Create: `frontend/src/auth/useKsaiToggle.ts`
- Modify: `frontend/src/auth/UserMenu.tsx`
- Modify: `frontend/src/auth/AuthContext.tsx`

- [ ] **Step 1: Hook que mutates el flag**

Crear `frontend/src/auth/useKsaiToggle.ts`:

```typescript
import { useState } from 'react';
import { apiFetch } from './api';
import { useAuth } from './AuthContext';

export function useKsaiToggle() {
  const auth = useAuth();
  const [saving, setSaving] = useState(false);

  async function setEnabled(enabled: boolean) {
    setSaving(true);
    try {
      const res = await apiFetch('/usuarios/me/ksai', {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ enabled }),
      });
      if (!res.ok) throw new Error('No se pudo actualizar');
      const data = await res.json();
      auth.updateUser?.({ ksai_enabled: data.ksai_enabled });
    } finally {
      setSaving(false);
    }
  }

  return { enabled: auth.user?.ksai_enabled ?? true, setEnabled, saving };
}
```

- [ ] **Step 2: Asegurar `updateUser` en AuthContext**

En `frontend/src/auth/AuthContext.tsx`, buscar la firma de `AuthContextValue`. Si no existe `updateUser`, agregarla:

```typescript
// En types.ts (interface AuthContextValue):
updateUser?: (patch: Partial<User>) => void;
```

Y en la implementación del provider:

```typescript
const updateUser = useCallback((patch: Partial<User>) => {
  setUser(prev => prev ? { ...prev, ...patch } : prev);
}, []);
```

Y en el objeto que se pasa al `<AuthContext.Provider value={...}>`, agregar `updateUser`.

- [ ] **Step 3: Agregar el toggle al dropdown**

En `frontend/src/auth/UserMenu.tsx`, dentro del dropdown (entre el bloque del avatar/email y el botón "Cerrar sesión"), agregar:

```tsx
<div className="px-4 py-3 border-t border-stone-800/70">
  <div className="text-[10px] uppercase tracking-[0.14em] text-stone-500 mb-2">Configuración</div>
  <KsaiToggleRow />
</div>
```

Y al final del archivo, antes del `function Avatar`, agregar:

```tsx
function KsaiToggleRow() {
  const { enabled, setEnabled, saving } = useKsaiToggle();
  return (
    <div className="flex items-center justify-between gap-3">
      <div className="min-w-0 flex-1">
        <p className="text-xs text-stone-200">Evaluación KSpace-AI</p>
        <p className="text-[10px] text-stone-500 leading-snug mt-0.5">
          Tus reflexiones son evaluadas automáticamente y el Calendario muestra una lectura mensual.
        </p>
      </div>
      <button
        type="button"
        role="switch"
        aria-checked={enabled}
        disabled={saving}
        onClick={() => void setEnabled(!enabled)}
        className={`relative inline-flex h-5 w-9 rounded-full transition-colors shrink-0 ${
          enabled ? 'bg-amber-300/70' : 'bg-stone-700'
        } ${saving ? 'opacity-60 cursor-wait' : 'cursor-pointer'}`}
      >
        <span
          className={`inline-block h-4 w-4 rounded-full bg-stone-950 shadow transform transition-transform mt-0.5 ${
            enabled ? 'translate-x-4' : 'translate-x-0.5'
          }`}
        />
      </button>
    </div>
  );
}
```

Y agregar el import al tope:

```typescript
import { useKsaiToggle } from './useKsaiToggle';
```

- [ ] **Step 4: Type-check + verificar**

Run desde `frontend/`:

```bash
npx tsc --noEmit
```

Expected: exit 0.

Verificar en el navegador: abrir el UserMenu (avatar arriba a la derecha), ver el toggle "Evaluación KSpace-AI". Cambiarlo on/off — debería persistir tras recargar.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/auth/useKsaiToggle.ts frontend/src/auth/UserMenu.tsx frontend/src/auth/AuthContext.tsx
git commit -m "feat(frontend): toggle KSpace-AI en el UserMenu (PATCH /usuarios/me/ksai)"
```

---

### Task 15: Nota informativa en el editor de reflexión

**Files:**
- Modify: `frontend/src/espejo/components/ReflectionEditor.tsx`

- [ ] **Step 1: Agregar la nota debajo del textarea**

En `frontend/src/espejo/components/ReflectionEditor.tsx`, agregar el import:

```typescript
import { useAuth } from '../../auth';
```

Y dentro del componente, antes del `return`:

```typescript
const auth = useAuth();
const ksaiEnabled = auth.user?.ksai_enabled ?? true;
```

Localizar el `<textarea>` o `<motion.textarea>` donde el usuario escribe la reflexión. Inmediatamente DESPUÉS del textarea (antes del botón submit), agregar:

```tsx
<p className="text-[10px] text-stone-500 mt-1 italic">
  {ksaiEnabled
    ? 'Tu reflexión es evaluada por KSpace-AI.'
    : 'KSpace-AI desactivado. Activalo en tu perfil para evaluación automática.'}
</p>
```

- [ ] **Step 2: Type-check + verificar**

Run desde `frontend/`:

```bash
npx tsc --noEmit
```

Expected: exit 0.

Verificar en el navegador: abrir Mi Árbol de la Vida, clickear una sefirá, abrir el editor de reflexión. Ver la nota. Cambiar el toggle del UserMenu y volver — el texto debería actualizarse.

- [ ] **Step 3: Commit**

```bash
git add frontend/src/espejo/components/ReflectionEditor.tsx
git commit -m "feat(frontend): nota KSpace-AI en el editor de reflexion"
```

---

### Task 16: Smoke test end-to-end + push

**Files:** (ninguno nuevo, validación manual)

- [ ] **Step 1: Backend tests completos**

Run desde `backend/`:

```bash
./venv/Scripts/python.exe -m pytest -v
```

Expected: todos los tests pasan (incluyendo los nuevos de KSpace-AI).

- [ ] **Step 2: TSC completo del frontend**

Run desde `frontend/`:

```bash
npx tsc --noEmit
```

Expected: exit 0.

- [ ] **Step 3: Smoke manual con stub (sin GEMINI_API_KEY)**

Levantar backend y frontend si no están corriendo. Verificar:

1. Loguearse. Abrir UserMenu → toggle KSpace-AI visible en "on" (default).
2. Ir al Calendario. Card de lectura aparece. Si no hay reflexiones del mes → status `no_data`. Si hay reflexiones de scores bajos → status `weak` con chips (mensaje será null porque estamos en stub).
3. Crear una actividad en una sefirá que esté floja → toast aparece arriba a la derecha.
4. Apagar el toggle. La card del Calendario cambia a status `disabled` con CTA al perfil. La nota del editor de reflexión cambia.

- [ ] **Step 4: Smoke con Gemini real (opcional, requiere GEMINI_API_KEY)**

Setear en `.env` del backend:

```
LLM_PROVIDER=gemini
GEMINI_API_KEY=<tu-key-de-google-ai-studio>
```

Reiniciar backend. Volver a probar el flujo. Esperar:
- `/evaluate` devuelve un score + feedback realista (no el stub).
- `/ia/calendario/lectura` con sefirot flojas devuelve un mensaje en español rioplatense, observacional.

- [ ] **Step 5: Push**

```bash
git push origin feat/gcal-sync
```
